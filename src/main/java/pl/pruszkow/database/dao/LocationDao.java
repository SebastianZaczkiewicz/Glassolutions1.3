package pl.pruszkow.database.dao;

import com.j256.ormlite.support.ConnectionSource;

public class LocationDao extends CommonDao {

    public LocationDao(ConnectionSource connectionSource) { super(connectionSource);}
}
